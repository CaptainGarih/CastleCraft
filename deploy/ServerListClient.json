#       Client

every 5 seconds:

    set {_count} to 0
    set {_server} to {global_config.server/name}
    loop all players:

        add 1 to {_count}
    
    make bungee server named "lobby" execute console command "serverlist %{_server}% %{_count}%"